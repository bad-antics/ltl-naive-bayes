// examples/demo.ltl — runnable demo for ltl_naive_bayes

import std::io
use ltl_naive_bayes as lib

fn main() {
    io::println("ltl_naive_bayes demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}