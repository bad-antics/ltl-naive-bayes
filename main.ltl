// ltl-naive-bayes/main.ltl
//
// Gaussian / Bernoulli / multinomial naïve Bayes.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-naive-bayes v0.1.0")
    io::println("Gaussian / Bernoulli / multinomial naïve Bayes.")
    0
}
